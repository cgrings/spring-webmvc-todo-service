spring:
  data:
    web:
      pageable:
        one-indexed-parameters: true
management:
  endpoints:
    web:
      exposure:
        include: "info,health,metrics"
    jmx:
      exposure:
        include: ""
        exclude: "*"
